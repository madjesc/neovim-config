-- local function didChangeConfiguration(config)
--     print(config)
-- end

vim.g.android_sdk_path = '~/.android-sdk/'

return {
    -- handlers = {
    --     ['workspace/didChangeConfiguration'] = didChangeConfiguration,
    -- }
}
